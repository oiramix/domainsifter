{
  "version": "1.0",
  "tlds": {
    "_doc": ".com re-added to approved on 2026-05-26 after the OVH KS-6 migration (128 GB RAM, 16-core EPYC 7351p) validated 5 consecutive clean natural-cron runs 2026-05-22 → 2026-05-26 on the new architecture (Phase 4 snapshot classifier, RDAP 429 cooldown handling, keyword denylist Round 1, per-TLD display cap). Pre-.com memory_peak holds at 3.87 GB; .com parse is expected to push to 15-25 GB during set-build, still well within budget. Cold-start handled by scripts/diff.py — first .com run writes the R2 baseline only (0 drops reported); first real .com diff lands 2026-05-28. The original 2026-05-10 OOM that forced .com to pending happened on the 16 GB GHA runner; that hardware constraint is gone now that the pipeline runs on the dedicated OVH host. .shop moved to pending on 2026-06-01: GMO RDAP IP-block (rdap.gmoregistry.net rejects our OVH IP at the Cloudflare edge with instant 429), whitelist request pending with info@gmoregistry.com since 2026-05-30. Today's run spent ~45 min (09:43–10:27) fighting GMO 429 cooldowns and produced 0 available / 23 not_available / 275 unknown from the bucket; temporary removal stops the daily time bleed. Re-add to approved when the whitelist is granted — verified via IANA RDAP bootstrap that .shop is the only currently-approved TLD routed through rdap.gmoregistry.net (the other 45 GMO-served TLDs are brand/geo TLDs we don't have CZDS access to). Carryover .shop entries already in daily-domains.json will age out naturally over the 14-day rolling window; no backfill needed.",
    "approved": [
      "app",
      "dev",
      "live",
      "studio",
      "tech",
      "online",
      "site",
      "store",
      "xyz",
      "info",
      "org",
      "biz",
      "net",
      "com"
    ],
    "pending": ["shop"]
  },
  "filter_thresholds": {
    "min_wayback_snapshots": 1,
    "max_domain_length": 30,
    "min_domain_length": 2,
    "min_open_page_rank": 0
  },
  "scoring_weights": {
    "_doc": "Relative weights; the formula in score.py averages over POPULATED components only so missing fields don't drag the denominator. cc_source_domain_count joined 2026-05-14 at 0.30 — symmetric with wayback because PLAN.md frames both as historical-authority signals (wayback = temporal evidence the site existed; cc = link evidence others thought it mattered). Domains absent from the CC graph (cc_source_domain_count=null) are excluded from the average rather than penalized.",
    "wayback_snapshots": 0.3,
    "open_page_rank": 0.4,
    "cert_history": 0.2,
    "domain_length": 0.1,
    "cc_source_domain_count": 0.3
  },
  "_keyword_lists_doc": "Keyword blocklist with three REJECT categories plus one SOFT-SIGNAL category. (1) rejected_keywords: exact-match against tokens split on dots/hyphens/digit-letter boundaries. (2) rejected_keyword_prefixes: startswith() against coarse tokens (curated subset of unambiguous abuse stems — 'cam'/'tube'/'sex'/'slot'/'poker'/'pirate' are intentionally NOT here because their English-word overlap was the 2026-05-17 migration's whole motivation). (3) rejected_keyword_substrings (added 2026-05-24, Round 1): substring match on the apex label, for scam-vertical stems where real-world spam concatenates the keyword into a longer compound name with no separator and no digit boundary (richmendating, miraclepills, onlinepharmacy) — none of those reach the token matchers. Curation rule for this list: entries MUST be ≥5 chars and compound-specific enough that incidental English-word overlap is rare. Known minor FPs: 'dating' catches 'updating', 'pills' catches 'pillsbury' — accepted because such names rarely drop. (4) soft_signal_keywords: do NOT reject — output.py reads them to force a 'Caution' verdict on dating/snake-oil/get-rich-quick/crypto-speculative names; 'dating' and 'hookup' are dead entries here as of 2026-05-24 (the substring reject fires first) but kept to avoid scope creep.",
  "rejected_keywords": [
    "sex", "sexy", "sexo", "sexe", "porn", "porno", "nude", "naked",
    "dildo", "fetish", "hentai", "milf", "tube", "webcam", "camgirl",
    "cam", "xtube", "xvideo", "xnxx", "redtube", "youporn", "pornhub",
    "r34", "e621", "xxx", "adult", "escort",
    "viagra", "cialis", "weed", "cbd", "kratom", "marijuana", "cannabis",
    "steroid", "opioid", "pharma",
    "casino", "gambling", "betting", "poker", "slot", "roulette",
    "blackjack", "baccarat", "bet365", "togel", "judi", "sbobet", "pkv",
    "tinbongda", "bolaonline",
    "repelis", "putlocker", "fmovies", "123movies", "soap2day",
    "couchtuner", "solarmovie", "streameast", "1337x", "pirate", "cuevana",
    "gnula", "pelisplus", "pelispedia",
    "stresser", "booter", "ipstress", "ddos", "nightmarestress",
    "scam", "fraud", "phishing", "warez", "torrent", "freebnb", "freeeth",
    "airdropclaim",
    "nazi", "jihad", "terrorist"
  ],
  "rejected_keyword_prefixes": [
    "porn", "porno", "hentai", "milf", "dildo", "xtube", "xvideo", "xnxx",
    "redtube", "youporn", "pornhub", "r34", "e621", "xxx",
    "roulette", "blackjack", "baccarat", "bet365", "togel", "judi",
    "sbobet", "pkv", "tinbongda", "bolaonline",
    "repelis", "putlocker", "fmovies", "soap2day", "couchtuner",
    "solarmovie", "streameast", "cuevana", "gnula", "pelisplus", "pelispedia"
  ],
  "rejected_keyword_substrings": [
    "dating", "forex", "hookup", "payday", "pharmacy", "pills",
    "replica", "weightloss"
  ],
  "soft_signal_keywords": [
    "dating", "hookup", "singles", "tinder", "romance",
    "miracle", "cure", "detox", "cleanse",
    "getrich", "instantcash", "easymoney",
    "pump", "moonshot", "shitcoin", "memecoin"
  ],
  "lexical_thresholds": {
    "max_digit_ratio": 0.30,
    "min_vowel_ratio": 0.15,
    "max_shannon_entropy": 3.5,
    "max_repeat_run": 4,
    "max_consonant_run": 6,
    "min_trigram_match_ratio": 0.55,
    "min_alpha_trigram_matches": 3,
    "min_apex_length": 3
  },
  "availability_check": {
    "_doc": "Per-host bucket caps for the RDAP availability stage, derived from a runtime budget. For each distinct RDAP host bucket, cap = floor(max_runtime_per_host_seconds * workers / effective_throttle), where effective_throttle comes from api_min_interval_seconds.rdap_per_host[host] -> rdap -> 2.0, and workers from rdap_concurrency.per_host[host] -> default_workers_per_host -> 1. Buckets exceeding their cap are random-shuffled (seed = today's date int) then truncated. global_cap is a safety net against pathological days where filter behaviour leaks far more survivors than expected; today's typical 14k lexical survivors fits below it. Replaced the prior length-asc trim (max_candidates_for_enrichment=1000) on 2026-05-04 — that heuristic biased toward already-registered short names without improving Wayback-signal density. max_runtime_per_host_seconds bumped 900 -> 2700 on 2026-05-05 to compensate for slower throttles after the ban events; per-host candidate volume stays similar (each bucket still wall-clock-bounded, now at 45 min not 15 min). Total RDAP phase wall-clock = max(bucket_runtimes) since all buckets run concurrently, so the slowest bucket caps total at ~45 min. NOTE: max_runtime_per_host_seconds is intentionally kept aligned with the top-level availability_budget_seconds (2700 s as of 2026-05-07). availability_budget_seconds is the global wall-clock deadline applied across the entire RDAP phase inside validate_availability(); when it's smaller than max_runtime_per_host_seconds, it pre-empts per-host completion and behaves as a guillotine (the bug 2026-05-07 surfaced — 1500 s was firing before any bucket finished its planned 2700 s). With both at 2700 s the global deadline acts as a redundant safety net rather than the binding constraint. 2026-05-09: Verisign (.com/.net) joined as a single shared bucket at rdap.verisign.com → 4.0s throttle (Mario's choice over agent's 3.0s recommendation given 70% blast radius if banned + undocumented Verisign rate-limit policy + ToS 'high volume automated processes' prohibition). Cap math: floor(2700 / 4.0) = 675 candidates/day for the combined .com+.net bucket — small fraction of expected ~30-35k daily lexical survivors but ban-prevention takes priority over volume on first deployment. Loosen to 3.0s only after 1-2 weeks of clean Verisign runs. Same date, GMO (rdap.gmoregistry.net) bumped 4.0 -> 5.0 defensively after 2026-05-09 cron logged 8 Retry-After:0 soft warnings — today's per-registry throttle audit flagged GMO as the most-exposed of currently-clean hosts; soft warnings argue for tightening, never loosening. Audit also established that 5 of 8 calibrated registries (Verisign, PIR, Identity Digital, GoDaddy/.biz, Radix) publish no numeric rate limit and rely on discretionary ToS enforcement; only CentralNic publishes a numeric ceiling (1800 q/15min = 2 q/s, /24 IPv4 aggregation). 2026-05-10: first Verisign-enabled run never reached the availability check — OOM-killed at 6m54s during the upstream .com zone parse. Root cause is in scripts/zone_parser.py, not in this stage: parse_zone builds a Python set[str] of all apex names in memory, and .com's ~160M apex names exceed the 16 GB GHA runner RAM ceiling (peak ~15.5 GB just for today_set; day-2 yesterday_set diff would push to ~30 GB). .com reverted to tlds.pending pending OVH KS-6 dedicated-server migration ordered today (128 GB RAM, 16-core AMD EPYC 7351p). .net stays in tlds.approved (~13M apex, comparable to .org scale, parses fine on GHA). The cap math in this _doc remains correct for all currently-approved TLDs; the OOM was a zone_parser memory-architecture issue, not a cap-math issue. .com returns to approved once OVH migration validates the pipeline runs cleanly at .com scale.",
    "max_runtime_per_host_seconds": 2700,
    "global_cap": 15000
  },
  "dns_check": {
    "_doc": "DNS pre-filter, run AFTER lexical filter and BEFORE RDAP bucket assignment. Queries NS records for each candidate apex via the system resolver (Quad9 since 2026-05-12 — see 'DNS resolver fix and DNSBL three-state contract' section in STATE.md). NXDOMAIN → dns_available=True → proceed to RDAP. Any NS records returned → dns_available=False → reject (the registry still delegates the domain, so it isn't actually available). Lookup error (timeout, transport failure, NoAnswer with name-exists-but-no-NS, NoNameservers) → dns_available=None → proceed to RDAP (fail-open, same epistemic-honesty pattern as the DNSBL three-state contract). Architectural intent: the RDAP throttle budget is the bottleneck on TLD scale-up — adding .com (~10× the next-largest TLD's apex count) would require either large RDAP throttle budgets (risks ban) or accept losing volume to skipped_budget. A genuinely-dropped domain has had its NS records removed at the registry, so a DNS NS query is a free, parallelizable pre-RDAP gate that should reject the 80-95% of post-lexical candidates that still have DNS infrastructure (in-transfer, in-grace-period, parked etc.) — letting RDAP focus its strict throttle budget on candidates that look genuinely available. Accuracy-preservation: candidates RDAP would have approved as available (404) have no NS, so DNS pre-filter rejects only candidates RDAP would have rejected too. Net effect: same final list, much less RDAP load. Defaults chosen 2026-05-12 (first deployment, conservative): 20 workers (~600 qps peak burst against Quad9 anycast — Quad9 publishes no formal limit but the DNSBL fix made them load-bearing for spam classification too, so don't poke them unnecessarily; raise later if production proves benign), 3s per-query timeout (covers slow TLD authoritative responses; the per-call wall-clock budget is workers × candidates × timeout / workers ≈ candidates × timeout but in practice dominated by sub-100ms responses), no throttle (set throttle_seconds > 0 if Quad9 fair-use starts biting). Set enabled=false to bypass the pre-filter entirely (escape hatch if dnspython misbehaves or Quad9 has an outage). Module: scripts/dns_prefilter.py — pipeline-stage helper, not an enricher (different lifecycle: runs once across all candidates with its own pool sizing; not registered in ENRICHMENT_MODULES).",
    "enabled": true,
    "workers": 20,
    "timeout_seconds": 3,
    "throttle_seconds": 0.0
  },
  "cc_backlinks": {
    "_doc": "Common Crawl domain-webgraph integration. WIRED INTO PIPELINE as of 2026-05-14 — `scripts/cc_refresh.py` builds and uploads per-release artifacts to R2; `scripts/enrichment/cc_backlinks.py` is registered in ENRICHMENT_MODULES and runs as part of the daily enrichment phase. Strategy A (latest release only — see STRATEGIC_NOTES.md `Multi-release CC query strategy`). On first call per pipeline process, the derived SQLite for `latest_release` is downloaded from R2 to `~/.cache/domainsifter/cc/` and reused for every subsequent candidate (sub-ms point-lookups thereafter). `source_domain_count=null` from the enricher means 'domain not in this release's graph' — null-aware in score.py (excluded from average) and intentionally NOT counted toward `publish_min_enrichment_completeness` so absence from CC graph doesn't penalize a candidate at the publication gate. Accumulation strategy: monthly CC releases never deleted; raw vertices+edges (~21 GiB/release) live on R2 IA tier (S3-API class name `STANDARD_IA`, matches AWS conventions — NOT Cloudflare's Workers-API `InfrequentAccess` spelling, which 400'd on the first OVH run 2026-05-13) ($0.010/GB-mo, rarely re-read after derived build), derived SQLite (~1.5 GiB/release) lives on Standard tier ($0.015/GB-mo, read once per pipeline run). Year-end cost projection: ~$3/mo at year 1, ~$14/mo at year 5 (60 releases × $0.23/each). Schema: one SQLite per release with `meta` table (release, built_at, schema_version, source_url) and `cc_apex` table (apex_domain TEXT PK, source_domain_count INTEGER) — danglers included as count=0 for three-state symmetry (not-in-graph vs zero-inbound are different facts). Release column intentionally NOT per-row — file path encodes the release context, meta table self-documents. Refresh cadence: manual `python -m scripts.cc_refresh --release <name>` invocation, then bump `latest_release` here and commit; monthly cron deferred (see STRATEGIC_NOTES.md `Common Crawl refresh cadence`).",
    "latest_release": "cc-main-2026-feb-mar-apr",
    "r2_raw_key_template_vertices": "cc/raw/{release}/vertices.txt.gz",
    "r2_raw_key_template_edges": "cc/raw/{release}/edges.txt.gz",
    "r2_derived_key_template": "cc/derived/{release}.sqlite",
    "source_url_template": "https://data.commoncrawl.org/projects/hyperlinkgraph/{release}/domain/{release}-domain-{file}.txt.gz"
  },
  "newsletter": {
    "_doc": "Daily newsletter draft generation via Buttondown. Enabled 2026-05-14 same-day as the wire-in commit — drafts are inherently safe (status=draft never reaches subscribers without a manual dashboard action), so flipping the flag on doesn't create deliverability risk. Idempotent: re-running the same day finds the existing draft by subject and skips creation. See STATE.md 'Daily newsletter — 2026-05-14' for the test-send workflow. Module: scripts/generate_newsletter.py — run-daily.sh invokes it AFTER pipeline.py succeeds, failure here is logged but never aborts the daily run. Disable by flipping enabled to false if Buttondown is misbehaving or for any reason draft creation should pause.",
    "enabled": true,
    "top_n": 20,
    "subject_template": "DomainSifter daily picks — {date}",
    "intro_text": "Today's top expired domain picks, sorted by score. Backlinks counts come from Common Crawl's last 3 months of crawl data. All domains were available at last check — verify at registrar before buying.",
    "site_url": "https://domainsifter.com"
  },
  "max_candidates_for_publication": 300,
  "publish_min_score": 30,
  "publish_min_enrichment_completeness": 0.50,
  "phase2": {
    "_doc": "LLM-based name-quality ranker (Haiku) inserted between the lexical filter and the RDAP availability check. Replaces the per-host random-shuffle trim with quality-ordered selection. Wires LIVE in tomorrow's production run (enabled=true on 2026-06-02). Fallback is wired to FAILURE ONLY — never to thin yield. Triggers: API error, cost-ceiling-hit-before-min-eligible, uncaught exception, missing ANTHROPIC_API_KEY. Per product policy 'smaller but cleaner is better', min_eligible defaults to 10 so a thin-but-valid day publishes a short quality list rather than reverting to a longer mechanical one. Budget enforcement is two-layer: (a) planned, via target_n = floor(daily_budget_usd / cost_per_1000_planning_usd * 1000); (b) runtime, a thread-safe cost meter halts further batches at 95% of daily_budget_usd. Pricing constants in scripts/phase2_ranker.py PRICE_*_PER_MTOK; cost_per_1000_planning_usd is the BUDGETED rate (with retry-pass + drift headroom over the diagnostic-measured ~$0.19/1k). Overflow (above-gate candidates that don't fit any RDAP bucket) is persisted to R2 at state/phase2_overflow.jsonl with first_seen_date, aged out at 14 days — same rolling window as carryover. NOT committed to the repo: this is the public domainsifter repo and overflow is private pick-pool data. Tunable: daily_budget_eur (cap), score_gate (eligibility threshold), min_eligible (fallback floor — keep low to honor thin-but-quality days), batch_size (smaller batches reduce Haiku JSON truncation; 15 is post-diagnostic calibration). Diagnostic that validated the prompt + cost + quality lives in scratch/haiku_ranker_diagnostic/ (untracked).",
    "enabled": true,
    "daily_budget_eur": 3.0,
    "eur_to_usd": 1.08,
    "cost_per_1000_planning_usd": 0.25,
    "score_gate": 60,
    "min_eligible": 10,
    "model": "claude-haiku-4-5-20251001",
    "batch_size": 15,
    "concurrency": 6,
    "max_retry_passes": 1
  },
  "display_caps": {
    "_doc": "Per-TLD ceiling applied at the DISPLAY layer only (homepage Today/Recent panels and the Buttondown newsletter top-N). Pipeline output daily-domains.json stays score-pure — this knob never touches the JSON contract. Rationale (2026-05-25): the published list is heavily .net-skewed because .net has Wayback content where other approved TLDs often don't. With ~232 entries across 14 TLDs and ~170 .net, a naive score-desc top-30 panel was rendering 25+ .net rows and creating an honesty gap against the 'curated across 15 TLDs' methodology copy. Algorithm: bucket by TLD, take top-N per TLD by score (slack flows naturally — TLDs with fewer than N just contribute what they have), flatten, score-sort, take panel size. Cap fires INDEPENDENTLY per surface: Today panel, Recent panel, newsletter top-N each see their own bucket. Tighten the cap (8→6) only if smaller TLDs still get crowded out; loosen (8→12) only if production data shows the cap is starving small-TLD slots without improving diversity. The newsletter and homepage may diverge later but share the same 8 for now (homepage panel=30, newsletter panel=20 from newsletter.top_n).",
    "max_per_tld_in_top_panel": 8
  },
  "verdict_thresholds": {
    "_doc": "Tightened 2026-05-17. Promising used to be earned by score >= 40 alone; the tag became meaningless when ~all published rows wore it (89/89 on 2026-05-16). New bar requires significant historical web presence (wayback >= 1000) AND at least one external authority signal (OPR >= 1.5 OR CC backlinks >= 10). Candidates clearing only the score threshold demote to Caution instead. Clean (the top tier) is unchanged — score >= 70 wins. Soft-signal keyword matches (dating, snake-oil, get-rich-quick, crypto-speculative) force Caution regardless of the above. Verdict is computed in scripts/output.py:_compute_verdict and serialized as a JSON field — frontend and newsletter read it instead of recomputing from score (single source of truth).",
    "clean_min_score": 70,
    "promising_min_score": 40,
    "promising_min_wayback_snapshots": 1000,
    "promising_min_open_page_rank": 1.5,
    "promising_min_cc_source_domain_count": 10
  },
  "enrichment_time_budget_seconds": 3000,
  "availability_budget_seconds": 2700,
  "diagnostic_logging": false,
  "registrars": [
    {
      "name": "Namecheap",
      "link_template": "https://namecheap.pxf.io/WO655J?u=https%3A%2F%2Fwww.namecheap.com%2Fdomains%2Fregistration%2Fresults%2F%3Fdomain%3D{name}"
    },
    {
      "name": "NameSilo",
      "link_template": "https://www.namesilo.com/domain/search-domains?query={name}&rid=36a0644du"
    },
    {
      "name": "Dynadot",
      "link_template": "https://www.dynadot.com/domain/search?domain={name}&rscreg=domainsifter"
    }
  ],
  "api_endpoints": {
    "czds_auth_base": "https://account-api.icann.org",
    "czds_api_base": "https://czds-api.icann.org",
    "wayback_cdx": "https://web.archive.org/cdx/search/cdx",
    "open_page_rank": "https://openpagerank.com/api/v1.0/getPageRank",
    "safe_browsing": "https://safebrowsing.googleapis.com/v4/threatMatches:find",
    "crtsh": "https://crt.sh",
    "rdap_bootstrap": "https://data.iana.org/rdap/dns.json"
  },
  "request_timeout_seconds": 10,
  "api_request_timeout_seconds": {
    "_doc": "Per-enricher request timeout. Falls back to request_timeout_seconds when unset. Wayback CDX and crt.sh frequently take 15-30s under peak load — their 10s default forced ~80% of calls to time out on 2026-04-30. The retry-on-timeout logic in wayback.py / crtsh.py adds a second safety net.",
    "wayback": 60,
    "crtsh": 60
  },
  "download_timeout_seconds": 120,
  "max_concurrent_enrichments": 1,
  "rdap_concurrency": {
    "_doc": "Per-host concurrency for the RDAP availability check. The pipeline buckets candidates by RDAP host and runs each bucket in its own ThreadPoolExecutor; all buckets execute simultaneously. Within a bucket, workers serialize on the existing thread-safe HostThrottle so per-host request rate never exceeds api_min_interval_seconds.rdap (or rdap_per_host) — concurrency only ever fans out *across* hosts. Defaults are safe (1 worker per host); per_host stays empty until empirically validated against a specific registry. Lookup: per_host[host] → default_workers_per_host → 1.",
    "default_workers_per_host": 1,
    "per_host": {}
  },
  "api_min_interval_seconds": {
    "_doc": "Minimum interval between requests to each API host. With max_concurrent_enrichments=1 (sequential) and multiplicative 0.75-1.25× jitter applied per call, actual rates are ~25% slower than the nominal value. Tightened 2026-04-30 after 503/502 storms with the previous 1.0s/0.4s/0.2s settings. RDAP global default raised 0.4 → 2.0 on 2026-05-05 after multiple registries banned us (identitydigital 24h, PIR 1h) under the previous default — a new/unknown registry must default to a safe rate.",
    "wayback": 3.0,
    "crtsh": 3.0,
    "open_page_rank": 1.0,
    "rdap": 2.0,
    "rdap_per_host": {
      "_doc": "Per-host overrides for RDAP. Falls through to api_min_interval_seconds.rdap when host not listed. Recalibrated 2026-05-05 after the first uncapped run produced multiple Retry-After bans: identitydigital.services 24-hour (1069 checked, 949 unknown), publicinterestregistry.org 1-hour (1966 checked, 1628 unknown), GMO 49 unknowns at 3.0s. Conservative empirical settings with safety margin — accept reduced volume in exchange for zero ban risk going forward, because a permanent ban from any registry would block the pipeline for that TLD entirely. Sources per host: identitydigital banned today → 5.0s (most cautious); PIR banned today → 3.0s (matches GMO caution); GMO 49 unknowns at 3.0s → 4.0s defensive bump; Google + nic.biz degraded but not banned → 2.0s; centralnic + radix clean at 0.4s but only 2250 candidates each so 1.0s defensive margin. 2026-05-09 changes: rdap.verisign.com added at 4.0s for the .com/.net rollout (Mario's choice over agent's 3.0s recommendation; reasoning lives in availability_check._doc). rdap.gmoregistry.net 4.0 -> 5.0 after today's cron logged 8 Retry-After:0 soft warnings; the per-registry throttle audit flagged GMO as the most-exposed of currently-clean hosts. Audit findings (5 of 8 registries publish no numeric limit; only CentralNic has a documented ceiling at 2 q/s with /24 IPv4 aggregation) confirm that calibration here is empirical-only for every host except centralnic.",
      "rdap.gmoregistry.net": 5.0,
      "rdap.identitydigital.services": 5.0,
      "rdap.publicinterestregistry.org": 3.0,
      "rdap.verisign.com": 4.0,
      "pubapi.registry.google": 2.0,
      "rdap.nic.biz": 2.0,
      "rdap.centralnic.com": 1.0,
      "rdap.radix.host": 1.0
    }
  },
  "rdap_429_backoff_floor_seconds": {
    "_doc": "Minimum seconds to wait after an RDAP 429 before the single retry, AND the per-host cooldown window during which further requests to that host are skipped without an HTTP call. Added 2026-05-22 after the GMO bucket logged a 47.6% unknown rate (257 unknown / 540 candidates) driven by a retry-during-cooldown cascade: the prior 1s+2s backoff retried entirely inside GMO's documented 60s cooldown, and GMO escalates non-compliance to temporary IP blocking. On a 429, scripts/enrichment/_circuit_breaker.py honors max(parsed Retry-After header, floor); GMO sends a useless literal 'Retry-After: 0' so the floor is what binds. Lookup: per_host[host] -> default. The gmoregistry value 60 is authoritative — verbatim from rdap.gmoregistry.net/help ('do not access any more for at least a minute to avoid temporary blocking of your address'). default 5 is a conservative small floor for hosts with no documented cooldown. This knob is ORTHOGONAL to api_min_interval_seconds (steady-state pacing): the 5.0s GMO steady-state throttle is unchanged — a 1 req/sec probe on 2026-05-22 confirmed GMO does not rate-limit at steady state, so the cascade was never a steady-state-rate problem.",
    "default": 5,
    "per_host": {
      "rdap.gmoregistry.net": 60
    }
  },
  "output_path": "src/data/daily-domains.json"
}
